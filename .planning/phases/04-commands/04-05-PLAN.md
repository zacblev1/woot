---
phase: 04-commands
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/commands/commands/info.ts
  - lib/commands/commands/style.ts
  - lib/commands/__tests__/info.test.ts
  - lib/commands/__tests__/style.test.ts
autonomous: true

must_haves:
  truths:
    - "about returns author information"
    - "contact returns email and social links"
    - "projects opens GitHub in new tab"
    - "theme lists or sets terminal theme"
    - "font lists or sets terminal font"
    - "neofetch displays system info"
  artifacts:
    - path: "lib/commands/commands/info.ts"
      provides: "5 info commands: about, contact, projects, whoami, date"
      exports: ["aboutCommand", "contactCommand", "projectsCommand", "whoamiCommand", "dateCommand"]
    - path: "lib/commands/commands/style.ts"
      provides: "3 style commands: theme, font, neofetch"
      exports: ["themeCommand", "fontCommand", "neofetchCommand"]
  key_links:
    - from: "lib/commands/commands/style.ts"
      to: "context.theme"
      via: "Theme operations"
      pattern: "context\\.theme\\."
    - from: "lib/commands/commands/style.ts"
      to: "context.font"
      via: "Font operations"
      pattern: "context\\.font\\."
---

<objective>
Implement info commands (about, contact, projects, whoami, date) and style commands (theme, font, neofetch).

Purpose: Info commands display static information about the site owner. Style commands allow customization of terminal appearance.

Output: 8 tested command definitions for info and style operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-commands/04-RESEARCH.md
@lib/commands/types.ts
@components/terminal.tsx (lines 1343-1397, 1646-1658 for reference implementations)
@lib/data/themes.ts
@lib/data/fonts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create info commands</name>
  <files>lib/commands/commands/info.ts</files>
  <action>
Create lib/commands/commands/info.ts with all 5 info commands:

```typescript
import type { CommandDefinition, TerminalLine } from '../types'
import { success } from '../types'

export const aboutCommand: CommandDefinition = {
  name: 'about',
  description: 'About the author',
  usage: 'about',
  execute: () => {
    return success([
      '',
      'Zachary',
      'Creative Technologist',
      '',
      'Building digital experiences that are thoughtful,',
      'accessible, and built to last.',
      '',
      'Interests: philosophy, programming, travel, making things',
      '',
    ])
  }
}

export const contactCommand: CommandDefinition = {
  name: 'contact',
  description: 'Contact information',
  usage: 'contact',
  execute: () => {
    // Return TerminalLine objects with href for links
    const lines: TerminalLine[] = [
      { type: 'output', content: '' },
      { type: 'link', content: 'Email    zachary@thefrenchjockey.com', href: 'mailto:zachary@thefrenchjockey.com' },
      { type: 'link', content: 'GitHub   github.com/zacblev1', href: 'https://github.com/zacblev1' },
      { type: 'output', content: '' },
    ]
    return success(lines)
  }
}

export const projectsCommand: CommandDefinition = {
  name: 'projects',
  description: 'View projects',
  usage: 'projects',
  execute: (args, context) => {
    context.openUrl('https://github.com/zacblev1')
    return success('Opening GitHub...')
  }
}

export const whoamiCommand: CommandDefinition = {
  name: 'whoami',
  description: 'Display current user',
  usage: 'whoami',
  execute: () => {
    return success('zachary')
  }
}

export const dateCommand: CommandDefinition = {
  name: 'date',
  description: 'Display current date and time',
  usage: 'date',
  execute: () => {
    return success(new Date().toLocaleString())
  }
}
```
  </action>
  <verify>npx tsc --noEmit lib/commands/commands/info.ts</verify>
  <done>info.ts exports aboutCommand, contactCommand, projectsCommand, whoamiCommand, dateCommand</done>
</task>

<task type="auto">
  <name>Task 2: Create style commands</name>
  <files>lib/commands/commands/style.ts</files>
  <action>
Create lib/commands/commands/style.ts with all 3 style commands:

```typescript
import type { CommandDefinition, ThemeName, FontName } from '../types'
import { success, error } from '../types'

export const themeCommand: CommandDefinition = {
  name: 'theme',
  description: 'Change terminal color theme',
  usage: 'theme [name]',
  execute: (args, context) => {
    const themeName = args[0]?.toLowerCase() as ThemeName | undefined

    if (!themeName) {
      const themes = context.theme.list()
      const themeList = themes.map(key => {
        const config = context.theme.config(key)
        return `  ${key === context.theme.current ? '* ' : '  '}${key.padEnd(12)} ${config.name}`
      })
      return success(['', 'Available themes:', '', ...themeList, '', 'Usage: theme <name>', ''])
    }

    const validThemes = context.theme.list()
    if (!validThemes.includes(themeName)) {
      return error(`Unknown theme: ${themeName}. Type 'theme' to see available themes.`)
    }

    context.theme.set(themeName)
    const config = context.theme.config(themeName)
    return success(`Theme set to ${config.name}`)
  }
}

export const fontCommand: CommandDefinition = {
  name: 'font',
  description: 'Change terminal font',
  usage: 'font [name]',
  execute: (args, context) => {
    const fontName = args[0]?.toLowerCase() as FontName | undefined

    if (!fontName) {
      const fonts = context.font.list()
      const fontList = fonts.map(key => {
        const config = context.font.config(key)
        return `  ${key === context.font.current ? '* ' : '  '}${key.padEnd(12)} ${config.name}`
      })
      return success(['', 'Available fonts:', '', ...fontList, '', 'Usage: font <name>', ''])
    }

    const validFonts = context.font.list()
    if (!validFonts.includes(fontName)) {
      return error(`Unknown font: ${fontName}. Type 'font' to see available fonts.`)
    }

    context.font.set(fontName)
    const config = context.font.config(fontName)
    return success(`Font set to ${config.name}`)
  }
}

export const neofetchCommand: CommandDefinition = {
  name: 'neofetch',
  description: 'Display system information',
  usage: 'neofetch',
  execute: (args, context) => {
    const themeConfig = context.theme.config(context.theme.current)
    const fontConfig = context.font.config(context.font.current)
    const { collections } = context

    return success([
      '',
      '  zachary@home',
      '  ------------',
      `  Theme: ${themeConfig.name}`,
      `  Font: ${fontConfig.name}`,
      '  Shell: web/1.0',
      '  Terminal: browser',
      `  Books: ${collections.books.length}`,
      `  Vinyl: ${collections.vinyl.length}`,
      `  Hardware: ${collections.hardware.length}`,
      '',
    ])
  }
}
```
  </action>
  <verify>npx tsc --noEmit lib/commands/commands/style.ts</verify>
  <done>style.ts exports themeCommand, fontCommand, neofetchCommand</done>
</task>

<task type="auto">
  <name>Task 3: Test info and style commands</name>
  <files>lib/commands/__tests__/info.test.ts, lib/commands/__tests__/style.test.ts</files>
  <action>
Create lib/commands/__tests__/info.test.ts:

**about tests:**
- Returns array with author info
- Contains "Zachary" and "Creative Technologist"

**contact tests:**
- Returns TerminalLine array with links
- Contains email link with href
- Contains GitHub link with href

**projects tests:**
- Calls context.openUrl with GitHub URL
- Returns "Opening GitHub..." message

**whoami tests:**
- Returns "zachary"

**date tests:**
- Returns a string (date format)
- Contains current year

Create lib/commands/__tests__/style.test.ts:

**theme tests:**
- Lists themes when no argument provided
- Shows asterisk next to current theme
- Sets theme when valid name provided
- Returns error for unknown theme
- Calls context.theme.set with theme name

**font tests:**
- Lists fonts when no argument provided
- Shows asterisk next to current font
- Sets font when valid name provided
- Returns error for unknown font
- Calls context.font.set with font name

**neofetch tests:**
- Returns system info array
- Contains current theme name
- Contains current font name
- Contains collection counts

Mock context with:
```typescript
const mockContext = {
  theme: {
    current: 'lumon' as ThemeName,
    set: vi.fn(),
    list: () => ['lumon', 'dracula', 'gruvbox'] as ThemeName[],
    config: (name: ThemeName) => ({ name: name === 'lumon' ? 'Lumon' : name.charAt(0).toUpperCase() + name.slice(1) })
  },
  font: {
    current: 'jetbrains' as FontName,
    set: vi.fn(),
    list: () => ['jetbrains', 'fira', 'mono'] as FontName[],
    config: (name: FontName) => ({ name: name === 'jetbrains' ? 'JetBrains Mono' : name })
  },
  collections: {
    books: [{}, {}, {}],
    vinyl: [{}, {}],
    hardware: [{}],
  },
  openUrl: vi.fn(),
}
```

Test count target: ~20 tests total.
  </action>
  <verify>npm test -- lib/commands/__tests__/info.test.ts lib/commands/__tests__/style.test.ts</verify>
  <done>All info and style command tests pass</done>
</task>

</tasks>

<verification>
```bash
# All tests pass
npm test -- lib/commands/__tests__/info.test.ts lib/commands/__tests__/style.test.ts

# TypeScript compiles
npx tsc --noEmit

# Commands export correctly
grep -E "export const.*Command" lib/commands/commands/info.ts lib/commands/commands/style.ts
```
</verification>

<success_criteria>
- lib/commands/commands/info.ts exports 5 info commands
- lib/commands/commands/style.ts exports 3 style commands
- contact returns TerminalLine objects with href for links
- theme/font commands use context methods, not direct imports
- neofetch uses context to get current theme/font config and collection counts
- projects calls context.openUrl
- All commands are pure functions receiving context via parameter
</success_criteria>

<output>
After completion, create `.planning/phases/04-commands/04-05-SUMMARY.md`
</output>

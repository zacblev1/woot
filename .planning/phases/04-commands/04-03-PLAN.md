---
phase: 04-commands
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/commands/commands/filesystem.ts
  - lib/commands/__tests__/filesystem.test.ts
autonomous: true

must_haves:
  truths:
    - "mkdir creates new directories"
    - "touch creates new empty files"
    - "rm removes files and directories"
    - "All commands return appropriate error messages for invalid operations"
  artifacts:
    - path: "lib/commands/commands/filesystem.ts"
      provides: "3 filesystem commands as CommandDefinition objects"
      exports: ["mkdirCommand", "touchCommand", "rmCommand"]
  key_links:
    - from: "lib/commands/commands/filesystem.ts"
      to: "context.vfs"
      via: "VFS mutation operations"
      pattern: "context\\.vfs\\.(mkdir|touch|rm)"
---

<objective>
Implement filesystem mutation commands: mkdir, touch, rm.

Purpose: These commands allow users to create and remove files/directories in the virtual filesystem. They wrap VFS operations and handle errors.

Output: 3 tested command definitions for filesystem operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-commands/04-RESEARCH.md
@lib/commands/types.ts
@components/terminal.tsx (lines 1622-1645 for reference implementations)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create filesystem commands</name>
  <files>lib/commands/commands/filesystem.ts</files>
  <action>
Create lib/commands/commands/filesystem.ts with all 3 filesystem commands:

```typescript
import type { CommandDefinition } from '../types'
import { success, error } from '../types'

export const mkdirCommand: CommandDefinition = {
  name: 'mkdir',
  description: 'Create directory',
  usage: 'mkdir <directory>',
  execute: (args, context) => {
    const path = args[0]
    if (!path) return error('Usage: mkdir <directory>')

    const err = context.vfs.mkdir(path)
    if (err) return error(err)

    return success('')
  }
}

export const touchCommand: CommandDefinition = {
  name: 'touch',
  description: 'Create empty file',
  usage: 'touch <filename>',
  execute: (args, context) => {
    const path = args[0]
    if (!path) return error('Usage: touch <filename>')

    const err = context.vfs.touch(path)
    if (err) return error(err)

    return success('')
  }
}

export const rmCommand: CommandDefinition = {
  name: 'rm',
  description: 'Remove file or directory',
  usage: 'rm <path>',
  execute: (args, context) => {
    const path = args[0]
    if (!path) return error('Usage: rm <path>')

    const err = context.vfs.rm(path)
    if (err) return error(err)

    return success('')
  }
}
```

Note: The VFS operations return null on success and error string on failure. Commands translate this to CommandResult.
  </action>
  <verify>npx tsc --noEmit lib/commands/commands/filesystem.ts</verify>
  <done>filesystem.ts exports mkdirCommand, touchCommand, rmCommand</done>
</task>

<task type="auto">
  <name>Task 2: Test filesystem commands</name>
  <files>lib/commands/__tests__/filesystem.test.ts</files>
  <action>
Create lib/commands/__tests__/filesystem.test.ts with comprehensive tests.

Use the same createMockContext pattern from navigation tests. Mock VFS operations to control success/failure paths.

**mkdir tests:**
- Returns error without path argument ("Usage: mkdir <directory>")
- Returns success when vfs.mkdir returns null
- Returns error message when vfs.mkdir returns error string
- Calls vfs.mkdir with correct path

**touch tests:**
- Returns error without path argument ("Usage: touch <filename>")
- Returns success when vfs.touch returns null
- Returns error message when vfs.touch returns error string
- Calls vfs.touch with correct path

**rm tests:**
- Returns error without path argument ("Usage: rm <path>")
- Returns success when vfs.rm returns null
- Returns error message when vfs.rm returns error string
- Calls vfs.rm with correct path

Use vi.fn() to create mock functions and verify they are called with correct arguments.

Test count target: ~12 tests covering all paths.
  </action>
  <verify>npm test -- lib/commands/__tests__/filesystem.test.ts</verify>
  <done>All filesystem command tests pass with full coverage</done>
</task>

</tasks>

<verification>
```bash
# All tests pass
npm test -- lib/commands/__tests__/filesystem.test.ts

# TypeScript compiles
npx tsc --noEmit

# Commands export correctly
grep -E "export const (mkdir|touch|rm)Command" lib/commands/commands/filesystem.ts
```
</verification>

<success_criteria>
- lib/commands/commands/filesystem.ts exports all 3 commands
- Each command validates required arguments
- Commands properly translate VFS null/string returns to CommandResult
- Tests use mocks to verify VFS operations are called correctly
- Both success and error paths are tested
</success_criteria>

<output>
After completion, create `.planning/phases/04-commands/04-03-SUMMARY.md`
</output>

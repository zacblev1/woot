---
phase: 03-games
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/games/types.ts
  - components/games/number-game/logic.ts
  - components/games/number-game/index.ts
  - components/games/number-game/__tests__/logic.test.ts
  - components/games/rps-game/logic.ts
  - components/games/rps-game/index.ts
  - components/games/rps-game/__tests__/logic.test.ts
autonomous: true

must_haves:
  truths:
    - "Number game accepts guess input and returns too high/low/correct result"
    - "Number game tracks attempt count correctly"
    - "RPS game accepts rock/paper/scissors and determines winner"
    - "RPS game tracks score across rounds"
    - "Both games handle quit command"
  artifacts:
    - path: "components/games/types.ts"
      provides: "GameResult interface for all games"
      exports: ["GameResult"]
    - path: "components/games/number-game/logic.ts"
      provides: "Pure function number game logic"
      exports: ["handleInput", "createInitialData", "getStartMessage"]
    - path: "components/games/rps-game/logic.ts"
      provides: "Pure function RPS game logic"
      exports: ["handleInput", "createInitialData", "getStartMessage"]
  key_links:
    - from: "components/games/number-game/logic.ts"
      to: "lib/types/games.ts"
      via: "imports NumberGameData"
      pattern: "import.*NumberGameData.*from"
    - from: "components/games/rps-game/logic.ts"
      to: "lib/types/games.ts"
      via: "imports RPSGameData"
      pattern: "import.*RPSGameData.*from"
---

<objective>
Create the shared GameResult type and extract Number Game and RPS Game as pure function modules.

Purpose: These are the two simplest games (input -> immediate result pattern). Extracting them first establishes the module pattern that other games will follow.

Output:
- components/games/types.ts with GameResult interface
- components/games/number-game/ with logic.ts, index.ts, tests
- components/games/rps-game/ with logic.ts, index.ts, tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-games/03-RESEARCH.md
@lib/types/games.ts
@lib/hooks/useGameState.ts
@components/terminal.tsx (lines 897-937, 1217-1267 for current implementations)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GameResult type and Number game module</name>
  <files>
    components/games/types.ts
    components/games/number-game/logic.ts
    components/games/number-game/index.ts
    components/games/number-game/__tests__/logic.test.ts
  </files>
  <action>
1. Create `components/games/types.ts`:
   ```typescript
   export interface GameResult {
     output: string | string[]
     outputType?: 'output' | 'error' | 'success' | 'wordle'
     updatedData?: Record<string, unknown>
     endGame?: boolean
   }
   ```

2. Create `components/games/number-game/logic.ts`:
   - Import `NumberGameData` from `@/lib/types/games`
   - Import `GameResult` from `../types`
   - Export `createInitialData(): NumberGameData` - returns { target: random 1-100, attempts: 0, maxAttempts: 10, guesses: [] }
   - Export `getStartMessage(): string[]` - returns intro text
   - Export `handleInput(input: string, data: NumberGameData): GameResult`
     - Handle 'quit' -> endGame: true
     - Handle non-number -> error message
     - Handle guess: compare to target, return too high/low/correct
     - Include attempt count in response
     - Update guesses array in updatedData

3. Create `components/games/number-game/index.ts`:
   ```typescript
   export * from './logic'
   ```

4. Create test file with cases:
   - createInitialData returns target 1-100, attempts 0
   - getStartMessage returns array with 'NUMBER' in it
   - handleInput('quit', data) returns endGame: true
   - handleInput('abc', data) returns error message
   - handleInput with guess < target returns 'Too low'
   - handleInput with guess > target returns 'Too high'
   - handleInput with correct guess returns success + endGame
   - handleInput increments attempts in updatedData

Match the exact behavior from terminal.tsx lines 897-937.
  </action>
  <verify>
Run `npm test -- --run components/games/number-game` - all tests pass
Run `npx tsc --noEmit` - no type errors in new files
  </verify>
  <done>
Number game module exists with pure logic functions. Tests verify all input/output combinations match current terminal.tsx behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RPS game module</name>
  <files>
    components/games/rps-game/logic.ts
    components/games/rps-game/index.ts
    components/games/rps-game/__tests__/logic.test.ts
  </files>
  <action>
1. Create `components/games/rps-game/logic.ts`:
   - Import `RPSGameData` from `@/lib/types/games`
   - Import `GameResult` from `../types`
   - Export `createInitialData(): RPSGameData`
     - Returns { playerChoice: null, computerChoice: null, rounds: 0, playerWins: 0, computerWins: 0 }
   - Export `getStartMessage(): string[]`
     - Returns intro text matching terminal.tsx lines 1219-1225
   - Export `handleInput(input: string, data: RPSGameData): GameResult`
     - Handle 'quit' -> return final score, endGame: true
     - Handle invalid input -> error message
     - Handle rock/paper/scissors:
       - Generate random computer choice
       - Determine winner (rock beats scissors, etc.)
       - Update scores in updatedData
       - Return result string with choices and current score

2. Create `components/games/rps-game/index.ts`:
   ```typescript
   export * from './logic'
   ```

3. Create test file with cases:
   - createInitialData returns correct initial state
   - getStartMessage returns array with 'ROCK PAPER SCISSORS'
   - handleInput('quit', data) returns endGame + final score
   - handleInput('invalid', data) returns error
   - handleInput('rock' | 'paper' | 'scissors', data) returns valid result
   - Win/lose/tie logic is correct
   - Score updates correctly in updatedData

Note: For testing win/lose, mock Math.random or test with known data patterns.
Match exact behavior from terminal.tsx lines 1217-1267.
  </action>
  <verify>
Run `npm test -- --run components/games/rps-game` - all tests pass
Run `npm test -- --run components/games` - all game tests pass
Run `npx tsc --noEmit` - no type errors
  </verify>
  <done>
RPS game module exists with pure logic functions. Tests verify all input/output combinations including win/lose/tie scenarios.
  </done>
</task>

</tasks>

<verification>
1. `npm test -- --run components/games` passes all tests
2. `npx tsc --noEmit` shows no errors in new files
3. Each game module exports: handleInput, createInitialData, getStartMessage
4. GameResult type is used consistently across both modules
5. Tests cover: quit, invalid input, all game-specific cases
</verification>

<success_criteria>
- components/games/types.ts exports GameResult
- components/games/number-game/logic.ts has pure functions matching terminal.tsx behavior
- components/games/rps-game/logic.ts has pure functions matching terminal.tsx behavior
- All tests pass with good coverage of edge cases
- No direct state mutation in any game logic
</success_criteria>

<output>
After completion, create `.planning/phases/03-games/03-01-SUMMARY.md`
</output>

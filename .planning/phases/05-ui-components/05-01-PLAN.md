---
phase: 05-ui-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/terminal/types.ts
  - components/terminal/SyntaxHighlighter.tsx
  - components/terminal/TerminalLine.tsx
  - components/terminal/__tests__/TerminalLine.test.tsx
  - components/terminal/index.ts
autonomous: true

must_haves:
  truths:
    - "TerminalLine renders different line types (input, output, error, success, link, wordle) with correct styling"
    - "SyntaxHighlighter tokenizes input and highlights commands, paths, flags correctly"
    - "highlightLine formats output lines with headers, labels, and paths"
  artifacts:
    - path: "components/terminal/types.ts"
      provides: "Component-specific types and token definitions"
      exports: ["Token", "TokenType", "TerminalLineProps"]
    - path: "components/terminal/SyntaxHighlighter.tsx"
      provides: "Tokenization and rendering functions"
      exports: ["tokenizeInput", "renderTokens", "highlightInput", "highlightLine"]
    - path: "components/terminal/TerminalLine.tsx"
      provides: "Memoized single line display component"
      exports: ["TerminalLine"]
  key_links:
    - from: "components/terminal/TerminalLine.tsx"
      to: "components/terminal/SyntaxHighlighter.tsx"
      via: "imports highlightInput, highlightLine"
      pattern: "import.*highlightInput.*highlightLine.*SyntaxHighlighter"
---

<objective>
Create the foundational terminal display components: SyntaxHighlighter for tokenization logic and TerminalLine for rendering individual lines.

Purpose: Extract the complex syntax highlighting and line rendering logic from terminal.tsx into dedicated, testable components. These form the base layer for HistoryDisplay.

Output: TerminalLine component with SyntaxHighlighter functions, fully tested and ready for HistoryDisplay integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-components/05-RESEARCH.md
@components/terminal.tsx (lines 17-110 for highlighting, 1811-1857 for line rendering)
@lib/types/terminal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create terminal component types and SyntaxHighlighter</name>
  <files>
    components/terminal/types.ts
    components/terminal/SyntaxHighlighter.tsx
  </files>
  <action>
    Create `components/terminal/types.ts` with:
    - TokenType: 'command' | 'invalid' | 'path' | 'string' | 'flag' | 'number' | 'argument' | 'space' | 'text'
    - Token: { type: TokenType; value: string }
    - TerminalLineProps interface (line: TerminalLine)
    - VALID_COMMANDS constant array (extracted from terminal.tsx lines 24-28)
    - HEADER_KEYWORDS constant array (extracted from terminal.tsx line 50)

    Create `components/terminal/SyntaxHighlighter.tsx` with:

    1. `tokenizeInput(input: string, validCommands: string[]): Token[]`
       - Extract tokenization logic from terminal.tsx lines 1875-1938
       - Match whitespace, quoted strings, flags (--flag/-f), numbers, paths (~/.), words
       - First word gets 'command' or 'invalid' type based on validCommands
       - Pure function, no React dependencies

    2. `renderTokens(tokens: Token[]): ReactNode`
       - Map tokens to colored spans using Tailwind classes
       - command: "text-accent font-semibold"
       - invalid: "text-destructive"
       - path: "text-primary"
       - string: "text-yellow-400"
       - flag: "text-purple-400"
       - number: "text-orange-400"
       - argument: "text-muted-foreground"

    3. `highlightInput(text: string): ReactNode`
       - Extract from terminal.tsx lines 18-43
       - Parse "~/path $ command args" format
       - Return prompt in muted, command in accent/destructive, args in foreground

    4. `highlightLine(text: string): ReactNode`
       - Extract from terminal.tsx lines 46-110
       - Handle header keywords (NAME, SYNOPSIS, etc.) in accent+bold
       - Handle label lines (indented "Label:  value") with accent label, foreground value
       - Handle inline patterns (paths ~/..., arguments <...>, [...]
       - Return plain text if no matches
  </action>
  <verify>
    - File exists at components/terminal/types.ts with Token, TokenType, TerminalLineProps exports
    - File exists at components/terminal/SyntaxHighlighter.tsx with all 4 functions exported
    - No TypeScript errors: `npx tsc --noEmit`
  </verify>
  <done>
    Tokenization and highlighting logic extracted from terminal.tsx into pure functions and types
  </done>
</task>

<task type="auto">
  <name>Task 2: Create memoized TerminalLine component</name>
  <files>
    components/terminal/TerminalLine.tsx
  </files>
  <action>
    Create `components/terminal/TerminalLine.tsx`:

    1. Import memo from React, types from ./types, highlighters from ./SyntaxHighlighter

    2. Helper function `getColorClass(type: TerminalLineType): string`
       - 'input': "text-primary"
       - 'error': "text-destructive"
       - 'success': "text-accent"
       - 'link': "text-foreground"
       - 'wordle': "" (special rendering)
       - default: "text-foreground"

    3. Render function that handles each line type:
       - 'input': Call highlightInput(line.content)
       - 'link': Render anchor tag with href, hover:text-primary hover:underline
       - 'wordle': Split content by comma, render each mark:letter pair with colors
         - X: text-green-400
         - ?: text-yellow-400
         - other: text-muted-foreground
       - default: Call highlightLine(line.content)

    4. Wrap component with React.memo (lines rarely change)

    5. Base className: "whitespace-pre-wrap break-words break-all"

    Match exact behavior from terminal.tsx lines 1811-1856
  </action>
  <verify>
    - File exists at components/terminal/TerminalLine.tsx
    - Component is wrapped in React.memo
    - No TypeScript errors: `npx tsc --noEmit`
  </verify>
  <done>
    TerminalLine component renders all line types with correct styling, memoized for performance
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests and barrel export</name>
  <files>
    components/terminal/__tests__/TerminalLine.test.tsx
    components/terminal/index.ts
  </files>
  <action>
    Create `components/terminal/__tests__/TerminalLine.test.tsx`:

    1. Test tokenizeInput:
       - Empty string returns empty array
       - Valid command "ls" returns [{ type: 'command', value: 'ls' }]
       - Invalid command "xyz" returns [{ type: 'invalid', value: 'xyz' }]
       - "ls books" returns command + space + argument tokens
       - "cd ~/books" returns command + space + path tokens
       - "--flag" alone returns flag token
       - "echo 42" returns command + space + number tokens
       - Quoted string '"hello"' returns string token

    2. Test highlightInput:
       - "~/books $ ls" renders prompt muted, command accented
       - "~/books $ invalid" renders command destructive
       - No match returns plain text

    3. Test highlightLine:
       - "NAME" header returns accent+bold span
       - "  Label:  value" returns label in accent, value in foreground
       - "~/path" returns path in primary
       - "<argument>" returns muted span
       - Plain text returns as-is

    4. Test TerminalLine component:
       - type: 'input' renders with highlightInput
       - type: 'output' renders with highlightLine
       - type: 'error' has text-destructive class
       - type: 'success' has text-accent class
       - type: 'link' renders clickable anchor
       - type: 'wordle' renders colored letter pairs

    Create `components/terminal/index.ts`:
    ```typescript
    export { TerminalLine } from './TerminalLine'
    export { tokenizeInput, renderTokens, highlightInput, highlightLine } from './SyntaxHighlighter'
    export type { Token, TokenType, TerminalLineProps } from './types'
    export { VALID_COMMANDS, HEADER_KEYWORDS } from './types'
    ```
  </action>
  <verify>
    - Tests pass: `npm test -- components/terminal/__tests__/TerminalLine.test.tsx`
    - Exports work: Can import { TerminalLine, tokenizeInput } from barrel
    - No TypeScript errors: `npx tsc --noEmit`
  </verify>
  <done>
    TerminalLine and SyntaxHighlighter fully tested with clean barrel exports
  </done>
</task>

</tasks>

<verification>
1. `npm test -- components/terminal/__tests__/TerminalLine.test.tsx` passes all tests
2. `npx tsc --noEmit` shows no TypeScript errors
3. Imports work: `import { TerminalLine, tokenizeInput } from '@/components/terminal'`
4. SyntaxHighlighter functions match existing terminal.tsx behavior for all line types
</verification>

<success_criteria>
- [ ] types.ts exports Token, TokenType, TerminalLineProps, VALID_COMMANDS, HEADER_KEYWORDS
- [ ] SyntaxHighlighter.tsx exports tokenizeInput, renderTokens, highlightInput, highlightLine
- [ ] TerminalLine.tsx is memoized and handles all 6 line types
- [ ] All tests pass with full coverage of tokenization and rendering
- [ ] Barrel export at components/terminal/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-components/05-01-SUMMARY.md`
</output>
